<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.YPictures" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Creer le composant titre par defaut -->
    <style name="DefaultTextStyle">
        <item name="android:textColor">@color/LightGray</item>
        <item name="android:textSize">18sp</item>
        <item name="android:fontFamily">@font/aq</item>
    </style>

    <!-- Creer le composant Sous titre par defaut -->
    <style name="CustomSubTitle" parent="DefaultTextStyle">
        <item name="android:textColor">@color/DarkgGray</item>
    </style>

    <!-- Creer le composant titre par defaut -->
    <style name="CustomTitle" parent="CustomSubTitle">
        <item name="android:textSize">24sp</item>
    </style>

</resources>